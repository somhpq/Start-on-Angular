(

	function(){
		
		// this is the first sample code that has dependency injected into the module
		// the dependency in this code is angular.filter
		var module = angular.module('startAngular',['angular.filter']);

	}()
);